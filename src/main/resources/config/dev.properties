# Development environment overrides

# Application
app.url=https://dev-app.example.com
api.url=https://dev-api.example.com

# Browser & UI
browser=chrome
implicit.wait=5

# Logging
log.level=DEBUG

# Feature toggles
feature.newCheckout=true
