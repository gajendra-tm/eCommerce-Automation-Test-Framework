# QA environment overrides

# Application
app.url=https://qa-app.example.com
api.url=https://qa-api.example.com

# Browser & UI
browser=firefox
implicit.wait=10

# Logging
log.level=INFO

# Feature toggles
feature.newCheckout=false
